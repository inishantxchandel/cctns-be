PORT=4000

# Local PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_NAME=noc

# Never use true in production — use migrations instead.
DB_SYNCHRONIZE=false

# Set true to run pending migrations on app startup (optional; you can use npm run migration:run instead).
DB_MIGRATIONS_RUN=false

# Optional: plain password for npm run seed:mock-users (default: NocDev#2026)
# SEED_MOCK_PASSWORD=NocDev#2026

# JWT — required for auth (e.g. openssl rand -hex 32)
JWT_SECRET=83a3116a4d57cfc6b80e6c71eede28ff61ab5ded1791798e50fc8b4095e5a863
JWT_EXPIRES_IN=7d
